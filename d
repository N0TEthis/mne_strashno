#include <iostrem> 
using namespace std;
int main()
{
    setlocale(LC_ALL, "RU");
   int h;// перемнная 
    int n; // система счисления (:|)
    cin<< h; //вводим переменную 
    cin<< n; // вводим систему счисления 
    if(n == 2,8,10,16 && n>30) 
    {
      //ошибка 
    }
    else
    {
      // не ошибка 
    }
    
    return 0;  
    
 }
